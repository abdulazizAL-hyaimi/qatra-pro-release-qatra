# بوابة التوزيع التشغيلي وبدء الإنتاج

هذه الحزمة **مرشّح إنتاج** وليست موافقة آلية على التوزيع. اعتماد الإنتاج يتم مرة واحدة بعد نجاح المراحل التالية دون استثناء.

## 1. بناء الاختبار

1. ارفع المصدر إلى مستودع GitHub خاص.
2. شغّل `Qatra Pro - Build Debug APKs`؛ يجب نجاح فحص المصدر وAndroid Lint وبناء أربع APKs.
3. ثبّت كل APK على جهاز الدور الصحيح، ولا تستخدم نسخ Debug مع بيانات فعلية.

## 2. القبول الميداني

نفّذ جميع بنود `TEST_PLAN_AR.md` على أجهزة Android الفعلية، وعلى طابعة 58/80 مم والطابعة الورقية المستخدمة فعليًا. اختبر انقطاع التطبيق، إلغاء الحفظ، تكرار الاستيراد، ملفًا معدلًا، والدور الخطأ. وثّق النتيجة والتاريخ والجهاز والمختبر.

## 3. مفتاح الإنتاج

1. أنشئ مفتاح توقيع جديدًا خارج المشروع.
2. احتفظ بأصل المفتاح ونسخة احتياطية في موقعين آمنين، وكلمات المرور في مخزن منفصل.
3. أضف أسرار GitHub الأربعة الموضحة في `build-tools/BUILD_APK_WITH_GITHUB_AR.md`.
4. لا تضع المفتاح أو كلمات المرور أو مفتاح المزامنة في المصدر أو JavaScript.

## 4. بناء الإصدار الموقّع

بعد توقيع محضر القبول فقط، شغّل `Qatra Pro - Build Signed Release` وأدخل العبارة:

`READY_FOR_PRODUCTION`

لن يعمل المسار دونها. يجب أن ينتج أربع APKs موقعة، وقائمة SHA-256، وتقرير شهادة التوقيع، وبيان الإصدار.

## 5. ترتيب التثبيت والترحيل

1. من التطبيقات القديمة، صدّر JSON وتحقق من الملفات قبل إزالة القديم.
2. ثبّت APK الإدارة الجديد، وأنشئ اسم المستخدم وPIN الخاصين بالجهاز، ثم فعّل البصمة اختياريًا بعد نجاح الدخول.
3. رحّل JSON القديم قبل تفعيل الإنتاج، ثم أنشئ نسخة محمولة برمز استعادة وتحقق منها بعد إعادة تثبيت تجريبية.
4. أنشئ ملف الربط المشفر من الإدارة، واربط أجهزة الأدوار الأخرى. سلّم رمز الربط بقناة منفصلة.
5. أكمل الترحيل لكل دور، ثم فعّل «بدء التشغيل الفعلي» مرة واحدة.
6. بعد التفعيل، احذف ملفات JSON النصية بأمان، واستخدم الملفات المشفرة فقط.

## 6. بدء الإنتاج والمراقبة

- ابدأ بمجموعة صغيرة في أول يوم، وراجع أرصدة أول خمس عمليات وملفات التأكيد والطباعة يدويًا.
- قبل نهاية أول شهر، طابق عهد المحصلين والصندوق والبنك وميزان المراجعة، ثم نفّذ الإقفال فقط إذا كان فرق القيد والمركز المالي صفراً.
- خذ نسخة محمولة في نهاية اليوم، واحفظ الملف ورمز الاستعادة في موقعين منفصلين خارج الجهاز.
- عند ظهور شاشة «تم إيقاف الإدخال لحماية البيانات»، أوقف العمل على ذلك الجهاز ولا تعِد إدخال الحركة حتى تشخيص SQLite.
- لا توزع APK إذا تغيّرت بصمة الشهادة، أو فشل checksum، أو لم تنجح أي خطوة قبول.

## قرار الإطلاق

الحالة تكون `GO` فقط إذا: نجح البناء السحابي، نجحت الأربع APKs، اكتملت خطة القبول بما فيها البصمة والتصميم على الأجهزة الفعلية، تطابقت التواقيع والبصمات، وتم اختبار الاستعادة والطباعة والانقطاع. خلاف ذلك الحالة `NO-GO`.

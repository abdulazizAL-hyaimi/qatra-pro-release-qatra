# إعداد النسخ الاحتياطي على Google Drive

## ما الذي ينفذه التطبيق؟

- يطلب صلاحيتي `drive.file` و`drive.appdata`. الأولى تتيح له إدارة ملفات النسخ التي أنشأها فقط، والثانية لمساحة إعدادات مخفية خاصة بالتطبيق.
- ينشئ مجلدًا ظاهرًا باسم `Qatra Pro Backups`.
- يرفع نسخة SQLite كاملة خاصة بدور التطبيق باستخدام رفع قابل للاستئناف.
- يحتفظ بأحدث 10 نسخ لكل اسم مستخدم ودور.
- يدعم النسخ اليدوي، واليومي، والأسبوعي في وقت محلي يحدده المستخدم. قد يؤخر Android المهمة قليلًا عند تفعيل Doze أو توفير البطارية.
- لا يحفظ Google access token. يُشفّر ملف البيانات بمفتاح عشوائي، ويُحفظ مفتاح الاستعادة منفصلًا داخل `appDataFolder` المخفي، بينما يُحمى مفتاح الجدولة المحلي عبر Android Keystore.
- بعد حذف التطبيق يختار المستخدم حساب Google نفسه، ثم يدخل اسم المستخدم ونوع المستخدم فقط. لا توجد كلمة مرور أو رمز استعادة منفصل.
- ما زالت النسخ القديمة ذات الإصدار V1 قابلة للاستعادة بكلمة مرورها مرة واحدة، ثم يحولها التطبيق تلقائيًا إلى الأسلوب الجديد.

## إعداد Google Cloud قبل التوزيع

1. أنشئ مشروع Google Cloud مملوكًا للمنشأة وفعّل Google Drive API.
2. أكمل شاشة OAuth Branding باسم وشعار قطرة برو، ورابط سياسة الخصوصية وشروط الاستخدام.
3. أنشئ OAuth Client من نوع Android لكل معرف حزمة إنتاج مع بصمة SHA-1 لشهادة توقيع الإنتاج نفسها:

   - `com.meyah.rawdah.system`
   - `com.meyah.rawdah.reader`
   - `com.meyah.rawdah.collector`
   - `com.meyah.rawdah.cashier`

4. إذا كانت الاختبارات تستخدم Debug APK، افتح الملف `OAUTH-ANDROID-FINGERPRINTS.txt` الموجود داخل حزمة APK الناتجة من GitHub Actions. يحتوي الملف على معرفات الحزم الأربع ذات اللاحقة `.debug` وبصمة SHA-1 المطابقة لتلك الحزمة تحديدًا. أنشئ أربعة عملاء Android بالقيم الواردة فيه.
5. أضف حسابات الاختبار في شاشة OAuth ما دام التطبيق في وضع Testing. عند التوزيع العام انقل الشاشة إلى Production وأكمل أي تحقق تطلبه Google.
6. لا تضف Client Secret إلى Android أو JavaScript. ربط Android يعتمد على اسم الحزمة وبصمة SHA-1.

رابط التفعيل المباشر بعد اختيار المشروع الصحيح:

`https://console.cloud.google.com/apis/library/drive.googleapis.com`

يجب أن يظهر في الصفحة زر **MANAGE / إدارة** وليس **ENABLE / تفعيل**. ظهور زر التفعيل يعني أن OAuth قد ينجح، لكن أول طلب فعلي للنسخ سيُرفض وسيبقى التطبيق في حالة «لم تكتمل التهيئة».

## معالجة رسائل «غير مهيأ» و«تعذر الوصول»

- «تفويض Google: تم بنجاح» مع «مساحة النسخ: لم تكتمل التهيئة» يعني أن شاشة Google نجحت، لكن Drive API أو إحدى الصلاحيتين رفضت أول طلب REST.
- حدّث التطبيق إلى البناء الذي يعرض سبب رفض Google بدقة. يتعرف التطبيق على `SERVICE_DISABLED` و`accessNotConfigured` والصلاحيات الناقصة وسيعطي الإجراء الصحيح بدل رسالة 403 العامة.
- عند انتهاء أو تلف رمز الوصول المؤقت، يحذفه التطبيق تلقائيًا ويعيد طلب التفويض مرة واحدة؛ ولا يحذف أي نسخة من Drive.
- تأكد أن `drive.file` و`drive.appdata` موجودتان في **Google Auth Platform → Data Access**، وأن حساب الهاتف مضاف إلى **Audience → Test users** ما دام المشروع في وضع Testing.
- إذا كان الحساب تابعًا لـ Google Workspace وظهرت `domainPolicy`، فيجب على مشرف النطاق السماح لتطبيقات Drive أو استخدام حساب مسموح.
- لا تعتبر الاستعادة جاهزة حتى يظهر **آخر رفع ناجح** واسم ملف النسخة. مجرد قبول شاشة Google لا ينشئ نسخة احتياطية.

لإظهار SHA-1 لشهادة الإنتاج محليًا:

```bash
keytool -list -v -keystore qatra-release-new.jks -alias YOUR_ALIAS
```

## اختبار القبول المطلوب

1. اربط كل تطبيق بحساب Google اختباري وارفع نسخة أولى.
2. تحقق من ظهور مجلد `Qatra Pro Backups` وملف الدور الصحيح.
3. نفّذ حركة جديدة، ثم شغّل «رفع نسخة مشفرة الآن» وتحقق من إنشاء ملف أحدث.
4. اختبر حساب Google خاطئًا واسم مستخدم خاطئًا ودورًا خاطئًا؛ يجب ألا تتغير SQLite.
5. احذف تطبيقًا اختباريًا، ثبته من جديد بالتوقيع نفسه، أنشئ جلسة الدخول بالاسم نفسه، ثم استعد أحدث نسخة.
6. تحقق من المشتركين والمستخدمين والتكليفات والسندات والطباعة بعد الاستعادة.
7. فعّل جدولة يومية بوقت قريب، أغلق التطبيق، وتحقق من الرفع عند توفر الشبكة.
8. أغلق نافذة اختيار الحساب وتأكد أن التطبيق يوضح أن العملية لم تكتمل دون الادعاء أن الصلاحية أُلغيت، ثم ألغ الصلاحية فعليًا من حساب Google وتأكد أن المهمة تطلب إعادة الربط.

> شهادة Debug في GitHub Actions قد تتغير بين تشغيل وآخر. سجّل البصمة الموجودة في حزمة الاختبار التي ستثبتها فعليًا، ولا تنقل بصمة Debug إلى عملاء الإنتاج.

## حدود مهمة

- الاستعادة الجديدة تتطلب حساب Google الذي أنشأ النسخة، واسم المستخدم، والدور المطابق لنسخة التطبيق.
- حذف «بيانات التطبيق المخفية» لقطرة برو من إعدادات Google Drive يحذف مفتاح الاستعادة؛ عندها لن يمكن فك النسخ الجديدة. لذلك لا تحذف بيانات التطبيق من Drive يدويًا.
- تغيير مفتاح توقيع APK بعد إنشاء OAuth Clients يتطلب إضافة بصمة SHA-1 الجديدة في Google Cloud، وإلا يفشل التفويض.

# قطرة برو — المرحلة الأولى لمنصة الإنتاج المركزية

## ما تم تنفيذه

أضيفت طبقة خادم مستقلة مبنية على ASP.NET Core 8 مع PostgreSQL، لتكون الأساس الذي ستتصل به تطبيقات الإدارة والكاشف والمحصل والصندوق بدل الاعتماد الدائم على تبادل الملفات اليدوي فقط.

يشمل التنفيذ الحالي:

- تسجيل دخول مركزي باسم المستخدم وPIN.
- إصدار JWT لمدة 12 ساعة مع الدور ومعرّف مساحة العمل ورمز الموظف.
- تسجيل وربط كل جهاز بمستخدم محدد.
- نقطة مزامنة `push` تحفظ كل عملية بمعرّف UUID فريد لمنع التكرار.
- نقطة مزامنة `pull` تسحب التغييرات بالتسلسل من PostgreSQL.
- سجل تدقيق للأحداث الأمنية وعمليات المزامنة.
- عزل البيانات حسب `WorkspaceId`.
- تحديد معدل المحاولات على تسجيل الدخول والمزامنة.
- فحص صحة للخدمة وقاعدة البيانات.
- تشغيل PostgreSQL وAPI بواسطة Docker Compose.
- Workflow في GitHub Actions لبناء API والحاوية وفحص ملف Compose.

## الملفات الرئيسية

- `server/QatraPro.Api/Program.cs`
- `server/QatraPro.Api/QatraPro.Api.csproj`
- `server/QatraPro.Api/Dockerfile`
- `docker-compose.production.yml`
- `.env.production.example`
- `.github/workflows/build-production-platform.yml`

## تشغيل محلي على خادم اختبار

1. انسخ `.env.production.example` إلى `.env`.
2. غيّر جميع القيم الافتراضية، خصوصًا كلمة مرور PostgreSQL ومفتاح JWT وPIN المدير.
3. نفّذ:

```bash
docker compose --env-file .env -f docker-compose.production.yml up -d --build
```

4. افحص:

```bash
curl http://127.0.0.1:8080/health
```

## حدود هذه المرحلة

هذه المرحلة تنشئ الخادم وقاعدة البيانات ومسار المزامنة الآمن، لكنها لا تجعل APKs الحالية متصلة بالخادم تلقائيًا بعد. المرحلة التالية هي إضافة عميل مزامنة داخل تطبيقات Android مع طابور محلي في SQLite، إعادة المحاولة عند عودة الإنترنت، حل التعارضات، وشاشة مراقبة المزامنة في الإدارة.

لا يتم إعلان النظام جاهزًا للإنتاج الكامل قبل:

- نجاح بناء الـAPI والحاوية في GitHub Actions.
- ربط تطبيقات Android بالخادم.
- اختبار انقطاع الإنترنت وإعادة الإرسال ومنع التكرار.
- تجربة ميدانية على أجهزة الكاشف والمحصل والصندوق والطابعات.
- تفعيل HTTPS ونسخ احتياطي مجدول ومراقبة الخادم.

# إعداد المزامنة المشتركة عبر Google Drive

## الفرق بين النسخ الاحتياطي والمزامنة

- **نسخ Google Drive الاحتياطي** يحفظ نسخة كاملة خاصة بالتطبيق والحساب، ويستخدم عند الاستعادة الطارئة.
- **مركز المزامنة** ينقل ملفات تغيير صغيرة ومشفرة بين الإدارة والكاشف والمحصل والصندوق، ثم يدمجها في SQLite دون استبدال قاعدة البيانات.

لا تستخدم مجلد النسخ الاحتياطي كمجلد مزامنة.

## الإعداد لمرة واحدة

1. تنشئ الإدارة مجلدًا عاديًا في Google Drive باسم `QatraPro-Sync`.
2. تشارك الإدارة المجلد مع حسابات Google المعتمدة للمحصلين والكاشفين والصندوق بصلاحية محرر.
3. تثبت تطبيق Google Drive على كل جهاز، ويضاف الحساب الموحد إلى الهاتف.
4. من تطبيق الإدارة تنشئ الإدارة ملف ربط المزامنة وتسلمه لكل مستخدم مع رمز الربط عبر قناة منفصلة.
5. يستورد كل تطبيق ملف الربط المخصص لشبكة المنشأة.
6. يفتح المستخدم «مركز المزامنة» ويضغط «اختيار حساب Google الموحد»، ثم يختار البريد نفسه في التطبيقات الأربعة.
7. يضغط «اختيار المجلد المشترك». يفتح قطرة برو Google Drive عند الحساب المحدد، ثم يختار `QatraPro-Sync` نفسه.

إذا لم يظهر Google Drive، يوضح التطبيق أن تطبيق Drive غير مثبت أو غير مفعّل ويعرض زر فتحه/تثبيته. وعند تغيير الحساب يُلغى ربط المجلد القديم تلقائيًا لمنع مزامنة البيانات مع حساب غير مقصود.

ينشئ قطرة برو داخل المجلد مجلدات توجيه تلقائيًا:

```text
QatraPro-Sync/
├── to-admin/
├── to-reader/
├── to-collector/
└── to-cashier/
```

## التشغيل اليومي

1. يفتح المستخدم مركز المزامنة.
2. يضغط «رفع التحديثات الجديدة» لإرسال العمليات التي جُهزت في طابور التطبيق.
3. يضغط «تحميل تحديثات الإدارة والمستخدمين» لاستقبال الملفات المخصصة لدوره.
4. يعود إلى التطبيق ويراجع كل حزمة واردة قبل الدمج.

تطبيقات الكاشف والمحصل والصندوق تجهز العمليات الجديدة تلقائيًا عند فتح مركز المزامنة. أما الإدارة فتجهز تكليفات الكاشفين والمحصلين وإعداد الصندوق من صفحات الإدارة الخاصة بها؛ وبعد اعتماد التصدير تدخل الحزمة تلقائيًا في طابور Drive.

## الحماية ومنع التكرار

- لا يُرفع ملف SQLite ولا يُستبدل ملف قاعدة الإدارة.
- كل حزمة مشفرة وموثقة ومحددة لدور مستلم واحد.
- يرفض التطبيق الحزمة المعدلة أو حزمة شبكة أخرى.
- تحفظ SQLite معرّف الحزمة ومعرّف العملية وبصمة المحتوى، فلا تطبق العملية نفسها مرتين.
- عند نجاح الدمج ينشئ التطبيق تأكيدًا مشفرًا ويضعه في طابور الرفع إلى المرسل.
- لا يُحذف الملف البعيد إلا بعد نجاح مراجعته ودمجه أو ثبوت أنه مكرر.

## عند ضعف الإنترنت

تبقى الحزم في طابور الرفع المحلي حتى تنجح الكتابة إلى المجلد المشترك. وتبقى العمليات الواردة في صندوق مراجعة محلي حتى يعتمدها المستخدم. يمكن تكرار الضغط على الرفع أو التحميل بأمان.
